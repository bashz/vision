import * as exposed from '../src/index';

describe('Vizion', () => {
  describe('Scales', () => {
    it('should be exposed', () => {
      expect(exposed.Scale).toBeDefined();
    })
  })
})
